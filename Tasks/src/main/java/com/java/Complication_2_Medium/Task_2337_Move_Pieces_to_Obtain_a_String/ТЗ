Вам даны две строки, обе длиной n.

Каждая строка состоит только из символов 'L', 'R' и '_', где:
Символы «L» и «R» представляют собой части, где часть «L» может перемещаться влево только в том случае,
если есть пустое пространство непосредственно слева от нее,
а часть «R» может перемещаться вправо только в том случае, если есть пустое пространство непосредственно справа от него.

Символ '_' представляет собой пустое пространство, которое может быть занято любой из частей 'L' или 'R'.

Возвращает true, если можно получить цель строки, перемещая части строки, начиная любое количество раз.

В противном случае верните false.

Пример 1:
    Input:
        start = "_L__R__R_", target = "L______RR"
    Output:
        true
    Объяснение:
        Мы можем получить строковую цель с самого начала, выполнив следующие ходы:
            - Переместите первую часть на один шаг влево, начало становится равным "L___R__R_".
            - Переместите последнюю часть на один шаг вправо, начало становится равным "L___R___R".
            - Переместите вторую часть на три шага вправо, начало становится равным "L______RR".
            Поскольку можно получить строку цели с самого начала, мы возвращаем true.